pipeline:
  build:
    image: python
    commands:
      - apk add git
      - pip install -U platformio
      - pio update
      - pio run
      - pio run --target upload --environment gas_keller
  a-test-step:
    image: debian
    commands:
      - echo "Testing.."